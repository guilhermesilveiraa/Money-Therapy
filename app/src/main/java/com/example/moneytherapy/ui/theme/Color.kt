package com.example.moneytherapy.ui.theme

import androidx.compose.ui.graphics.Color

// Cor de Fundo Principal (Fundo geral da página)
val BackgroundPrimary = Color(0xFF121212) // Utilizada como a cor de fundo principal da aplicação, ideal para o fundo da tela.

// Cor de Fundo Secundária (Background de componentes como cards e caixas)
val BackgroundSecondary = Color(0xFF1C1C1E) // Usada em áreas de destaque, como cards, caixas e contêineres de gráficos.

// Cor de Texto Principal (Texto principal sobre fundo escuro)
val TextPrimary = Color(0xFFE5E5E5) // Cor clara para o texto principal, garantindo legibilidade sobre o fundo escuro.

// Cor de Texto Secundária (Texto neutro para informações menos importantes)
val TextSecondary = Color(0xFFA9A9A9) // Usada em subtítulos, textos de rodapé e informações secundárias para reduzir contraste excessivo.

// Cor de Acento Principal (Acento para botões e chamadas de ação)
val AccentPrimary = Color(0xFFB8860B) // Dourado escuro para botões principais e elementos que demandam atenção, remetendo a uma aparência premium.

// Cor de Acento Secundária (Para indicações positivas, como ganhos)
val AccentSuccess = Color(0xFF4CAF50) // Verde escuro para indicadores de sucesso, como sinais de crescimento, lucro ou mensagens de confirmação.

// Cor de Alerta (Indicação de perda, erro ou alertas)
val AccentError = Color(0xFFFF5C5C) // Vermelho suave usado para indicar quedas em gráficos, alertas ou mensagens de erro.

